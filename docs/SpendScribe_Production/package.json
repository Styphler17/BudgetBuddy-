{
  "name": "budgetbuddy-",
  "version": "1.0.0",
  "description": "1.  **Environment Variables:** Rename `.env.example` to `.env` and update your database credentials:\r     ```\r     DB_HOST=localhost\r     DB_NAME=your_database_name\r     DB_USER=your_database_user\r     DB_PASS=your_database_password\r     ```\r 2.  **Production Mode:** In `index.php`, ensure `define('APP_ENV', 'production');` is set to disable error displays.\r 3.  **Base URL:** The application automatically detects the `BASE_URL`, but ensure your `.htaccess` is supported by your hosting (Apache).\r 4.  **Database Migration:** Import the SQL schema from `docs/budgetbuddy.sql` into your production database.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build:css": "npx tailwindcss -i ./public/css/tailwind-input.css -o ./public/css/style.css --minify",
    "watch:css": "npx tailwindcss -i ./public/css/tailwind-input.css -o ./public/css/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Styphler17/BudgetBuddy-.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Styphler17/BudgetBuddy-/issues"
  },
  "homepage": "https://github.com/Styphler17/BudgetBuddy-#readme",
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^3.4.19"
  }
}
